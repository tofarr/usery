# Alembic migrations go here.

This file was added so that the directory would exist in git and there is not an error when
`poetry run alembic revision --autogenerate -m "Initial migration"` is initially run